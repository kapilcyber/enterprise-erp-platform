"""API Developer Portal domain entities — Phase 1 markers (ORM-free)."""

# Identity / catalog aggregates are represented as ORM models at the
# persistence boundary. Pure domain entities may be expanded later.
